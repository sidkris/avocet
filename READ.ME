# avocet

`avocet` is a Python package providing easy-to-use encryption and decryption functions. 

This library supports multiple methods, including Caesar Cipher, XOR Cipher, and AES encryption, making it suitable for a variety of cryptographic needs.

## Installation

To install `avocet`, simply use pip:

```bash
pip install avocet
